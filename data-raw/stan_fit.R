# model <- stan_model("data-raw/model.stan")
# stan_fit <- run_stan(
#     model = model,
#     data = list(y = 3),
#     chains = 4,
#     iter = 100
# )
#
# usethis::use_data(stan_fit, overwrite = TRUE)
